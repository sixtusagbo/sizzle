/// Where a toast is anchored on screen, which also sets its slide direction.
enum SizzlePosition {
  /// Anchored to the top; slides down into view. The default.
  top,

  /// Anchored to the bottom; slides up into view.
  bottom,
}
